describe(
	"login",
	{
		retries: {
			runMode: 3,
			openMode: 2,
		},
	},
	() => {
		it("should redirect unauthenticated user to sign-in page", () => {
			// ...
		})

		it("allows user to login", () => {
			// ...
		})
	}
)
